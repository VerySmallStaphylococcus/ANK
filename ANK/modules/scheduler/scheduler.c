#include "./scheduler.h"
#include "../work_system/work_system.h"

void sch64(ANK_WORK *works, size64 numof_work)   {
    //work loop
    for (size64 i = 0; i < numof_work; i++) {
        works[i];
    }
    
}